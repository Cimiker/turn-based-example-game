The sprites for cards are stored in the UnoCards.sprites directory.

The format for naming the cards is the following: <color>_<number/action>_<style>.
Cards that do not have a color, eg change color and plus 4, do not have the <style> tag in their name.
